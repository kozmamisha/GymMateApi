# Copy to .env and fill in. The .env file is git-ignored on purpose.
#
#   cp .env.example .env
#
# POSTGRES_PASSWORD must match the password used in the connection strings
# stored in User Secrets (see README - Local Development Secrets).

POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_PORT=5432
